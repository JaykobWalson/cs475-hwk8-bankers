int getNeedMatrix();//Sanity checks and gets the matrix.
int compareRow(int *rowOfNeed, int *work, int num_resources);//Compares a row to another to check if the number of work they do is the same
int sanityCheck();//sanity check used in getNeedMatrix()


